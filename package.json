{
  "name": "lbcd.cbd.int",
  "description": "LBCD",
  "dependencies": {
    "bower": "1.5.2",
    "express": "3.15.x",
    "http-proxy": "~1.9.0"
  },
  "devDependencies": {
    "awsbox": "0.6.x"
  },
  "engine": "node >= 0.10.18",
  "scripts": {
    "postinstall": "./node_modules/.bin/bower install --config.interactive=false",
    "start": "node server"
  }
}
